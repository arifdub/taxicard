// Name of the short-lived cookie that proves a reset link was just
// verified in this browser. Kept out of the server-action file, which may
// only export async functions.
export const RECOVERY_COOKIE = 'tc_recovery'
